apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ pvc_name }}
  namespace: monitoring-stack  # Add namespace here##################################### currently manually added
spec:
  accessModes:
    - {{ storageAccessMode }}
  resources:
    requests:
      storage: {{ storage_size }}
  storageClassName: {{ storageClassName }}