apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ pv_name }}
spec:
  capacity:
    storage: {{ storage_size }}
  accessModes:
    - ReadWriteOnce
  nfs:
    path: {{ export_path }}
    server: {{ hostvars[groups['server'][0]]['internal_ip'] }}