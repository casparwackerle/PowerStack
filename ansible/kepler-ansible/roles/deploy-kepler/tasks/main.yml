---
- name: Ensure Helm is installed
  shell: |
    curl https://raw.githubusercontent.com/helm/helm/master/scripts/get-helm-3 | bash
  args:
    creates: /usr/local/bin/helm

- name: Add {{ kepler_name }} repository and update Helm
  shell: |
    helm repo add {{ kepler_name }} {{ kepler_repo_url }}
    helm repo update

- name: Check if {{ kepler_namespace }} namespace exists
  command: kubectl get namespace {{ kepler_namespace }}
  register: namespace_check
  ignore_errors: true

- name: Create {{ kepler_namespace }} namespace if necessary
  when: namespace_check.rc != 0
  command: kubectl create namespace {{ kepler_namespace }}
  ignore_errors: true

- name: Ensure /var/lib/{{ kepler_name }} directory exists
  file:
    path: /var/lib/{{ kepler_name }}
    state: directory
    owner: root
    group: root
    mode: '0755'

- name: Check if {{ kepler_name }} already exists
  stat:
    path: /var/lib/{{ kepler_name }}/{{ kepler_name }}-installed
  register: tycho_installed

- name: Ensure {{ kepler_name }} values file is rendered locally
  template:
    src: tycho-values.yml.j2      # was kepler-values.yml.j2
    dest: /tmp/tycho-values.yml   # was /tmp/kepler-values.yml
  delegate_to: localhost
  become: false

- name: Debug rendered tycho values file
  command: cat /tmp/tycho-values.yml
  delegate_to: localhost
  run_once: true
  become: false

- name: Copy {{ kepler_name }} values file to the target machine
  copy:
    src: /tmp/tycho-values.yml
    dest: /tmp/tycho-values.yml
    mode: '0644'
  become: true

# # Public-facing: Helm release name is now "tycho"
# - name: Install {{ kepler_name }} using Helm
#   when: not tycho_installed.stat.exists
#   shell: |
#     helm install {{ kepler_name }} kepler/kepler \
#       --namespace {{ kepler_namespace }} \
#       --create-namespace \
#       -f /tmp/tycho-values.yml
#   environment:
#     KUBECONFIG: /home/ubuntu/.kube/config
#   become: true

- name: Deploy {{ kepler_name }} via Helm (idempotent)
  shell: |
    helm upgrade --install tycho kepler/kepler \
      --namespace tycho --create-namespace \
      --wait --timeout 10m \
      -f /tmp/tycho-values.yml
  environment:
    KUBECONFIG: /home/ubuntu/.kube/config
  become: true

- name: Mark {{ kepler_name }} as installed
  file:
    path: /var/lib/{{ kepler_name }}/{{ kepler_name }}-installed
    state: touch
  when: not tycho_installed.stat.exists
